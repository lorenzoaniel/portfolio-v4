import React from "react";
import styled from "styled-components";

const Home = () => {
	return <_Home.Main>Toggled On Test</_Home.Main>;
};

const _Home = {
	Main: styled.section`
		color: white;
		font-size: 3rem;
	`,
};

export default Home;
