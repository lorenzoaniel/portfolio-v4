import { motion } from "framer-motion";
import styled from "styled-components";

export const Main = styled(motion.h1)`
	margin-top: 5rem;
	font-weight: 200;
	font-size: 4.7rem;
	text-align: center;
	color: #ffffff;
`;
